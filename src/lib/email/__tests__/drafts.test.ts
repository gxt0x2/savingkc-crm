import {describe,expect,it} from 'vitest';import {canSendDraft} from '../drafts';describe('draft send guard',()=>it('requires ownership, current revisions and no suppression',()=>{expect(canSendDraft({isController:true,contentCurrent:true,controllerCurrent:true,suppressed:false})).toBe(true);expect(canSendDraft({isController:true,contentCurrent:false,controllerCurrent:true,suppressed:false})).toBe(false)}))
