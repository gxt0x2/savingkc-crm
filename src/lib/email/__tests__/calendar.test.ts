import {describe,expect,it} from 'vitest';import {schedulingMode} from '../calendar';describe('callback scheduling',()=>it('does not guess a calendar appointment',()=>{expect(schedulingMode({calendarConnected:true,exactRequest:false,slotVerified:true,hasPhoneEvidence:true})).toBe('task');expect(schedulingMode({calendarConnected:true,exactRequest:true,slotVerified:true,hasPhoneEvidence:true})).toBe('calendar')}))
