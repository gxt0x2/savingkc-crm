import {describe,expect,it} from 'vitest';import {audienceEligibility} from '../audiences';describe('audience eligibility',()=>it('requires evidence without creating a Lead',()=>{expect(audienceEligibility({hasEmail:true,identity:'confirmed',permission:true,verified:true,suppressed:false})).toBe('eligible');expect(audienceEligibility({hasEmail:true,identity:'unresolved',permission:true,verified:true,suppressed:false})).toBe('needs_review');expect(audienceEligibility({hasEmail:true,identity:'confirmed',permission:false,verified:true,suppressed:false})).toBe('excluded')}))
