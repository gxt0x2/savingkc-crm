import { describe,expect,it } from 'vitest'
import { assertIndependentSendingDomain,senderCanReceive,senderCanStartNewEnrollment } from '../providers/resend-domains'
describe('sending domains',()=>{it('rejects primary domain, subdomains and same registrable domain',()=>{expect(()=>assertIndependentSendingDomain('mail.savingkc.com','savingkc.com')).toThrow();expect(()=>assertIndependentSendingDomain('savingkc.co.uk','app.savingkc.co.uk')).toThrow();expect(assertIndependentSendingDomain('savingkc-mail.com','savingkc.com')).toBe('savingkc-mail.com')});it('retired senders keep receiving but cannot enroll',()=>{expect(senderCanStartNewEnrollment('retired')).toBe(false);expect(senderCanReceive('retired')).toBe(true)})})
