# EM-014 · Bridge email Leads, acquisition handoffs and CRM history

Status: Partial: canonical Lead creation/link, immutable message history, callback review task, attribution and explicit holds are connected and verified against disposable PostgreSQL. Handoff resolution/retry, acceptance/reassignment/outcomes, qualification and shared owner reconciliation remain unfinished. Audited 2026-09-12; see [current build status](../build-status.md), [CRM bridge checkpoint](../crm-bridge-checkpoint.md) and [local verification](../local-verification.md).

Read [v1.1 sales and response changes](../07-sales-voice-and-response-operations.md) before implementing any affected behavior.

## Outcome

Create one evidenced seller Lead and human handoff without unintended qualification or other-channel side effects.

## Dependencies and source contract

Required completed packets: [EM-004](EM-004.md), [EM-012](EM-012.md), [EM-013](EM-013.md).

Read [data/states](../02-data-and-states.md), [integrations/jobs](../03-integrations-and-jobs.md), [pages/actions](../04-pages-and-interactions.md), [AI policy](../05-ai-playbooks-and-evaluations.md) and [verification](../06-tests-and-release.md), focusing on **02 §1; 03 §9; 04 §7**. Use [manifest](../design-manifest.json) for exact dependency/action/test coverage. Read applicable AGENTS.md and installed framework guides before code.

## Allowed edit area

- `supabase/migrations/*_email_handoffs.sql`
- `src/lib/email/crm-adapter.ts`
- `src/lib/email/handoffs.ts`
- `src/lib/email/__tests__/legacy.test.ts`
- `src/lib/email/__tests__/handoffs.test.ts`
- `supabase/migrations/*_email_crm_compatibility.sql`
- `supabase/migrations/*_email_crm_bridge.sql`
- `supabase/migrations/*_email_crm_identity_guard.sql`
- `src/lib/email/crm-history.ts`
- `src/lib/email/crm-repairs.ts` (EM-029 internal projection retry boundary)
- `supabase/migrations/*_email_crm_projection_repairs.sql`
- `src/lib/email/workflow/{core,http,schedule,service,types}.ts`
- `src/lib/email/contracts.ts`
- `src/lib/email/commands/settings.ts` (hold the linked canonical task on access reduction)
- `supabase/migrations/20260912130000_email_configuration.sql` (unreleased profile/auth bootstrap correction)
- `tests/email-db/workflow.test.ts`
- `tests/email-local/{database.mjs,crm-fixture.sql,workflow.spec.ts}`

The connected implementation shares the existing workspace transaction/API and EM-024 component instead of introducing a second handoff store or endpoint. The CRM fixture loads the actual entity migration and checked work-item functions. The source/null-phone changes and narrow identity guard are unreleased compatibility drafts, not production approval.

Paths are repository-relative proposed ownership. Migration wildcard means a new migration named with actual execution date; inspect dependent FK/schema order first. Add focused tests beside named modules. If a necessary shared contract change falls outside this area, document it and update the owning contract/packet before implementing it; do not quietly expand scope or edit unrelated working changes.

## Required behavior

1. Resolve actual leads source/null-phone constraints additively; existing first touch and advanced/terminal stage retained.
2. Use contacted=Lead and qualified=Opportunity, acquisitions-only qualification evidence; no general auto-advance/PPC conversion calls.
3. Project activity once using canonical message IDs; callback task uses honest email source, no fake phone, dialing, SMS or invitations.

Owned action contracts: **THR-HANDOFF, HAN-ACCEPT, HAN-REASSIGN, HAN-OUTCOME, HAN-QUALIFY, HAN-RETURN**. Implement each exact payload, role, pending/success/error/conflict state and audit behavior from04. Shared handlers may be built here and consumed by later page packets.

## Failure and concurrency contract

Use 03's strict error envelope and durable receipts. No optimistic provider success, fake ready states, raw fallback AI sends or new idempotency keys for unresolved remote outcomes. Never derive actor/workspace authority from client fields. Preserve suppression, immutable versions, stable sender and shared thread controller. New inbound invalidates stale drafts. Display unknown/blocked state with evidence rather than inventing data to satisfy a test.

## Verification

Contributes evidence to acceptance cases: **T14, T31, T32, T35** from06. Verify the assertions implemented by this packet at the appropriate unit/DB/API level; record future UI/provider portions as pending. Only EM-017 and EM-030 claim their complete integrated journeys after all required evidence exists.

Base commands below are resolved against EM-001's actual package manager. Run additional explicit DB/provider/browser cases required above; a unit pass alone cannot replace them. Commands that depend on future harnesses become available after EM-002; never silently skip them.

```sh
npm run test:ci -- src/lib/email/__tests__/handoffs.test.ts
```

For API/schema/permission changes, include negative direct requests and relevant database tests. For UI, exercise populated, empty, error, stale and no-access states at narrow/wide widths using authenticated test identities; inspect focus, text wrapping and actual command reactions. For provider/model tests use explicit controlled recipients/cost cap and retain correlated evidence; default tests make no external sends. Run required release checks when this packet reaches a release candidate, not repeatedly after every cosmetic edit.

## Completion report and boundary

Record implementation SHA, changed files, exact checks/results, acceptance IDs, screenshot/provider/evaluation evidence where applicable, unverified limitations and next ready packet in [build-status](../build-status.md). Hand back a reviewable diff. Do not mark the packet done when its own required component evidence is missing; do not mark an entire T-case passed from partial component evidence. Do not deploy, migrate production, buy subscriptions/domains, send to customers or launch a campaign as a hidden consequence of this packet. Within an authorized build, complete reversible local work without repeated approval requests; surface only actual blocked external actions with the concrete result ready to review.

## Approved action workspace continuation

Read [revision 1.3](../09-action-workspace.md), which supersedes the earlier overlapping queue and passive callback-card design. Shared changes are owned here: `src/components/email/email-thread-panel.tsx`, `email-workspace.tsx` and its CSS; `src/lib/email/workflow/{service,types,presentation,callback-actions,schedule}.ts`; `src/lib/email/contracts.ts`; the new `20260913010000_email_action_workspace.sql` migration and corresponding unit/database/browser fixtures. THR-NOTE is owned by EM-013. Manual schedule/completion is owned by EM-024. These are local implementation additions, not a claim that this packet or its external dependencies are complete.
