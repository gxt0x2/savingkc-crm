# EM-032 · Add targeted phone and callback alerts with acknowledgment

Status: Partial: recipient-scoped local notifications, acknowledgment, backup escalation, a blocked push-test ledger and lockscreen/phone-signal helpers. Live device registration and push delivery remain unfinished. See [current build status](../build-status.md), [productization](../17-productization.md) and [local #3 report](../18-local-3-packet-complete.md).

## Outcome and dependencies

Send actionable recipient-scoped alerts and distinguish queued/delivered/acknowledged work.

Required component dependencies: [EM-012](EM-012.md), [EM-014](EM-014.md), [EM-023](EM-023.md), [EM-029](EM-029.md). Read [07 Sales voice and response operations](../07-sales-voice-and-response-operations.md), **07 §4; 04 NTF actions; 06 T51–T52**, and shared data/API/page/AI/release contracts02–06. Read applicable AGENTS.md and installed Next guides before app code. Prior provider checks may remain visibly blocked while independent local components proceed; full acceptance requires actual stated evidence.

## Allowed edit area

- `supabase/migrations/*_email_notifications.sql`
- `src/lib/email/notifications.ts`
- `src/lib/email/commands/notifications.ts`
- `src/lib/push-notifications.ts`
- `src/components/email/handoff-panel.tsx`
- `src/components/email/setup-wizard.tsx`
- `src/lib/email/__tests__/notifications.test.ts`
- `tests/email/notifications.spec.ts`

Add only new date-named migrations after actual schema preflight. Shared existing-file changes must preserve unrelated work. No production migration/deployment, purchase, staff message, customer email or call is performed as a side effect of this packet.

## Required behavior

1. Extract new phone/time signals from current message spans, not signatures/quoted history; bare phone alerts owner without fabricating Lead/Opportunity/appointment.
2. Persistent CRM work plus targeted push; verify auth-user mapping and device readiness. Do not blindly use the current unscoped generic notification feed or all-agent broadcast.
3. Create delivery/ack ledgers, dedupe events, propose5-minute urgent acknowledgment and backup escalation. Cancel obsolete reminders on transfer/reschedule.
4. Lockscreen payload stays nonsensitive. Missing delivery is a visible issue; no silent SMS/Slack/email fallback. Controlled test targets requesting user only.

Owned actions: **NTF-TEST, NTF-ACK**. Implement the exact roles, payloads, durable writes, pending/success/error/conflict states, audit and test cases in04. New requests use strict shared Zod schemas and durable receipts. New events/jobs have unique logical keys, fencing and retry semantics. Do not bypass suppression, budgets, identity resolution or human ownership.

## Verification and completion

Contributes component evidence to **T51, T52**. Unit/DB cases test real invariants, browser cases exercise the signed-in operator flow at narrow/wide widths; provider/device checks require explicit controlled scope and cannot be replaced by fixtures. Keep overall acceptance pending until the full evidence required in06 exists.

```sh
npm run test:ci -- src/lib/email/__tests__/notifications.test.ts
npm run test:email:db
```

Run this packet's browser specs once UI is available; use EM-001's actual package manager. Record implementation SHA, changed files, precise checks/results, current gaps and next ready packet in [build-status](../build-status.md). EM-030 remains the final release/evidence reconciliation task and depends on this packet. Finish the bounded reversible implementation within an authorized build; do not request repeated permission for local steps or silently enlarge external scope.

