# EM-034 · Connect one stable email-response phone line

Status: Partial: intended existing-number save only. Provisioning, routing and TEL-TEST remain unfinished; no number is purchased. See [current build status](../build-status.md) and [productization](../17-productization.md).

## Outcome and dependencies

Route email-origin incoming calls to the right team member, capture missed calls and preserve attribution.

Required component dependencies: [EM-024](EM-024.md), [EM-026](EM-026.md), [EM-028](EM-028.md), [EM-032](EM-032.md). Read [07 Sales voice and response operations](../07-sales-voice-and-response-operations.md), **07 §6; 04 TEL actions; 06 T55**, and shared data/API/page/AI/release contracts02–06. Read applicable AGENTS.md and installed Next guides before app code. Prior provider checks may remain visibly blocked while independent local components proceed; full acceptance requires actual stated evidence.

## Allowed edit area

- `src/lib/email/phone-line.ts`
- `src/lib/email/commands/phone-line.ts`
- `src/lib/twilio-numbers.ts`
- `src/lib/agent-routing.ts`
- `src/app/api/twilio-missed-call/route.ts`
- `src/components/email/sending-panel.tsx`
- `src/lib/email/__tests__/phone-line.test.ts`
- `tests/email/phone-line.spec.ts`

Add only new date-named migrations after actual schema preflight. Shared existing-file changes must preserve unrelated work. No production migration/deployment, purchase, staff message, customer email or call is performed as a side effect of this packet.

## Required behavior

1. Verify real number ownership/current purpose first; prefer a reusable existing number, protect reserved Ads and agent company lines. No purchase/provisioning from save.
2. Add explicit email_marketing number purpose, default blast/dialer eligibility off, stable return caller ID. Known owner routing, unknown intake, backup then business voicemail.
3. Use actual agent-leg outcomes and provider CallSid dedup; add voicemail/missed-call work and targeted alert once. Parent completed is not proof an agent answered.
4. Prevent inherited automatic SMS/lead/PPC-conversion side effects on this line. One shared number proves program origin, not exact campaign/person; keep unknown matches unresolved.
5. If incoming texts are advertised, verify existing SMS inbox ownership and coverage before publishing that contact option. No automatic dialing or text marketing.

Owned actions: **TEL-SAVE, TEL-TEST**. Implement the exact roles, payloads, durable writes, pending/success/error/conflict states, audit and test cases in04. New requests use strict shared Zod schemas and durable receipts. New events/jobs have unique logical keys, fencing and retry semantics. Do not bypass suppression, budgets, identity resolution or human ownership.

## Verification and completion

Contributes component evidence to **T31, T32, T52, T55**. Unit/DB cases test real invariants, browser cases exercise the signed-in operator flow at narrow/wide widths; provider/device checks require explicit controlled scope and cannot be replaced by fixtures. Keep overall acceptance pending until the full evidence required in06 exists.

```sh
npm run test:ci -- src/lib/email/__tests__/phone-line.test.ts
npm run test:email:db
```

Run this packet's browser specs once UI is available; use EM-001's actual package manager. Record implementation SHA, changed files, precise checks/results, current gaps and next ready packet in [build-status](../build-status.md). EM-030 remains the final release/evidence reconciliation task and depends on this packet. Finish the bounded reversible implementation within an authorized build; do not request repeated permission for local steps or silently enlarge external scope.

